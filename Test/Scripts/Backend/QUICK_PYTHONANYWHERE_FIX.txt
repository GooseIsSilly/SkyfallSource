================================================================================
🚨 QUICK FIX FOR YOUR 500 ERROR
================================================================================

ERROR: "Failed: Network error: HTTP/1.1 500 Internal Server Error"
YOUR URL: https://novafy.pythonanywhere.com/

PROBLEM: Your backend is crashing on PythonAnywhere

================================================================================
IMMEDIATE STEPS TO FIX (DO THIS NOW!)
================================================================================

Step 1: Check Your Error Log (MOST IMPORTANT!)
-----------------------------------------------
1. Go to https://www.pythonanywhere.com
2. Login
3. Click "Web" tab
4. Scroll to "Log files" section
5. Click "Error log" (red text)
6. **READ THE ERROR** - it tells you exactly what's wrong!

Copy the error and read it carefully.

================================================================================
COMMON ERRORS & FIXES
================================================================================

Error 1: "ModuleNotFoundError: No module named 'fastapi'"
----------------------------------------------------------
FIX: Install dependencies!

1. Go to "Consoles" tab
2. Click "Bash"
3. Run these commands ONE BY ONE:

pip3 install --user fastapi
pip3 install --user uvicorn[standard]
pip3 install --user bcrypt
pip3 install --user pydantic

Wait for each to finish!

Error 2: "No such file or directory: 'Accounts'"
-------------------------------------------------
FIX: Create folders!

In Bash console:
cd mysite
mkdir Accounts
mkdir Bans
mkdir Reports

Error 3: "NameError" or "ImportError"
--------------------------------------
FIX: Your Account.py has syntax errors

Check:
- All imports at top
- No typos
- Proper indentation

Error 4: "Can't find app"
-------------------------
FIX: WSGI file not configured

See Step 2 below!

================================================================================
Step 2: Fix Your WSGI Configuration
------------------------------------

1. Go to "Web" tab
2. Find "Code:" section  
3. Click "WSGI configuration file" link
4. DELETE EVERYTHING in the file
5. PASTE THIS (change YOURUSERNAME!):

```
import sys
import os

# Add your project directory
path = '/home/YOURUSERNAME/mysite'  # ← CHANGE THIS!
if path not in sys.path:
    sys.path.append(path)

os.chdir(path)

# Import your app
from Account import app
application = app
```

6. Change "YOURUSERNAME" to your actual PythonAnywhere username!
7. Save (Ctrl+S)

================================================================================
Step 3: Fix Account.py
----------------------

Your Account.py needs changes for PythonAnywhere!

CRITICAL FIX: Remove uvicorn.run()
----------------------------------
At the END of Account.py, FIND:

if __name__ == "__main__":
    uvicorn.run(app, host="0.0.0.0", port=8000)

CHANGE TO:

if __name__ == "__main__":
    pass

PythonAnywhere doesn't use uvicorn.run()!

CRITICAL FIX: Use absolute file paths
--------------------------------------
FIND lines like:
with open("Accounts/" + ID + ".json", "r") as savefile:

CHANGE TO:
import os
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

def get_path(*paths):
    return os.path.join(BASE_DIR, *paths)

# Then use:
with open(get_path("Accounts", ID + ".json"), "r") as savefile:

Do this for ALL file operations!

================================================================================
Step 4: Reload Web App
-----------------------

After making ANY changes:

1. Go to "Web" tab
2. Click big green "Reload novafy.pythonanywhere.com" button
3. Wait 10 seconds
4. Check error log again
5. Repeat until no errors!

================================================================================
Step 5: Test Your Backend
--------------------------

Open browser:
https://novafy.pythonanywhere.com/

Should see:
{"log":"MeteorBackend"}  ✓

If you see this, backend works!

If you still see error:
→ Check error log
→ Read the error message
→ Fix that specific error
→ Reload
→ Test again

================================================================================
COPY/PASTE FIXED ACCOUNT.PY TEMPLATE
================================================================================

I can't create .py files directly, but here's what to do:

1. Create new file "Account.py" on PythonAnywhere
2. Copy your existing Account.py code
3. Make these changes:

AT THE TOP (add imports):
```python
import json
import os
import random
import string
import bcrypt
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from pydantic import BaseModel
from datetime import datetime

app = FastAPI()

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)

# Get base directory
BASE_DIR = os.path.dirname(os.path.abspath(__file__))

def get_path(*paths):
    return os.path.join(BASE_DIR, *paths)

# Create folders
os.makedirs(get_path("Accounts"), exist_ok=True)
os.makedirs(get_path("Bans"), exist_ok=True)
os.makedirs(get_path("Reports"), exist_ok=True)

TokenDict = {}
ADMIN_KEY = "your_secret_admin_key_12345_CHANGE_THIS"
```

REPLACE ALL file operations:
```python
# OLD:
with open("Accounts/" + ID + ".json", "r") as savefile:

# NEW:
with open(get_path("Accounts", ID + ".json"), "r") as savefile:
```

AT THE BOTTOM:
```python
if __name__ == "__main__":
    pass  # PythonAnywhere uses WSGI
```

================================================================================
STILL NOT WORKING?
================================================================================

Copy your error log message and:

1. Read it carefully
2. Google the error
3. The error tells you EXACTLY what's wrong!

Common patterns:
- "No module named X" → Install X
- "No such file" → Create the file/folder
- "Permission denied" → Check file paths
- "Can't import app" → Fix WSGI file

================================================================================
YOUR UNITY SETTINGS ARE CORRECT!
================================================================================

Your BackendConfig.asset is already set correctly:
URL: https://novafy.pythonanywhere.com/
     ↑
     This is correct!

The problem is 100% on the PythonAnywhere backend side!

Once you fix the backend, Unity will work immediately!

================================================================================
CHECKLIST
================================================================================

[ ] Installed: fastapi, uvicorn, bcrypt, pydantic
[ ] Created folders: Accounts, Bans, Reports  
[ ] Fixed WSGI file with correct username
[ ] Removed uvicorn.run() from Account.py
[ ] Changed file paths to use get_path()
[ ] Clicked "Reload" button
[ ] Checked error log - no errors
[ ] Tested https://novafy.pythonanywhere.com/ - shows {"log":"MeteorBackend"}
[ ] Tested in Unity - login works!

Once all checked, it will work! ✓

================================================================================
MOST IMPORTANT: READ THE ERROR LOG!
================================================================================

The error log will tell you EXACTLY what's wrong!

Don't guess - read the actual error and fix that specific issue!

Good luck! You're close! 🚀

================================================================================
